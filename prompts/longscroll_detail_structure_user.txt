请基于以下内容生成电商长卷详情页“页面结构蓝图”。

【叙事结构 JSON】
{narrative_json}

【长卷视觉母版说明】
{visual_master_spec}

【本批切片数量】
{target_slice_count}

【整套页面模块比例计划】
{module_ratio_plan}

【本批模块任务】
{chunk_module_plan}

【结构修正要求（可选）】
{extra_requirements}

要求：
1. 严格输出 {target_slice_count} 个 items，slice_id 必须以【分批生成要求】和【本批模块任务】指定的编号为准。不得少屏，不得多屏，不得把 slice_id 重新从 "01" 编号。一个 slice_id 必须对应一个 item，禁止把两个 slice_id 合并成一个 item。
2. 必须严格执行【本批模块任务】里每个 slice_id 指定的 module_type、module_label、allowed_archetypes。
3. 每屏必须继承【本批模块任务】中的 screen_job 和 evidence_type，并围绕它们设计独立信息职责。
3A. 每屏必须执行【本批模块任务】中的 information_density、content_point_target、screen_fill_strategy、hierarchy_strategy 和 composition_shift。9:21 长屏默认需要复合内容结构，不能仍按“一屏只讲一个卖点”处理。特别是 slice_id 为 "03" 时必须是高密度：1 个强主模块 + 2-3 个辅助模块，形成 3-4 个清晰内容点。
4. 每屏必须选择一个 layout_archetype，只能从系统提示词提供的 12 个版式意图中选择，且必须落在本屏 allowed_archetypes 的语义范围内；layout_archetype 是信息组织方向，不是固定卡片模板。
5. 每屏必须有 commercial_goal 和 user_question，且相邻屏不能回答同一个 user_question。
6. 每屏必须有 primary_module，且 area_ratio 明确。
7. 至少 80% 的屏幕必须有 secondary_modules；function、detail、steps、parameter_trust 类型必须有 2 个或以上 secondary_modules。
8. 个护、按摩、穿戴、服饰、家居类产品，需要安排足够的人物使用或真实场景屏，但不能挤占功能、细节、步骤、参数/信任、FAQ/收口的配额。
9. 每张 9:21 屏幕建议承载 2-4 个内容点：1 个 primary_module + 1-3 个 secondary_modules；hero、faq_close 可以偏轻，但也必须有产品身份/利益/行动提示等至少 2 个有效信息点。
10. 相邻屏的 headline、commercial_goal、primary_module.message 不得高度相似；不要把同一卖点换句文案重复一遍。每屏 headline 必须是唯一最强记忆点，secondary_modules 只能做降级补充，不能和主标题抢层级。
11. 如果连续两屏都是 function，第一屏讲“机制/为什么有效”，第二屏讲“模式/结果/适配”；如果两屏都是 detail，必须分别讲不同细节证据。
11A. 连续 function/detail 屏禁止都使用“产品居中大图 + 周围图标/卡片”；至少一屏必须改成局部微距、人物使用、结构剖面、斜向动线、全幅场景融合或纵向序列。
12. 整套页面必须有文字信息密度节奏：场景/情绪/收口屏偏低密度，功能/步骤/参数屏可以中高密度；不要每屏同样满。
12A. 内容权重决定模块数量：核心利益、核心机制、关键功能等高权重屏可以有一个强主视觉，但仍要搭配证据/场景/利益补充；便携、适用场景、轻量 FAQ、信任短句、辅助卖点等低权重内容必须合并成 2-4 个内容点放在同一张 9:21 屏里，但只能在同一个 slice_id 内合并。
12B. 功能性内容允许“合并相关卖点”：例如一个核心功能 + 一个材质/结构证据 + 一个使用场景/利益结果，放在同一屏内形成饱满信息结构；禁止跨 slice_id 合并，禁止用一个 item 覆盖两屏。
13. 如果第 08 屏是 detail，只讲材质、结构、工艺、触感、关键部件等细节证据；禁止提前做参数表、包装清单、认证墙或售后承诺。
14. 如果第 09 屏是 parameter_trust，只讲规格、认证、安全、售后、配置、包装清单等理性购买确认；禁止复述第 08 屏的微距细节。
14A. 如果 module_type 是 steps，必须表达清晰的 2-3 个短步骤；可以用连续动作、局部过程、纵向动线或自然分区，禁止变成单个微距大图或单个 3:4 画面。
14B. 如果 module_type 是 parameter_trust，必须优先竖向信息层级：标题/信任短句、参数或认证信息、产品或材质背景融合；禁止横版网页式左右对半排版。
15. text_exact 必须使用简体中文，围绕叙事结构里的 key_message 展开成真实可见文案，不要改成英文。
16. visual_style_dna 是全局美术系统，不允许每屏换风格。
17. top_edge_anchor 和 bottom_edge_anchor 只定义安全区和视觉承接，不要在这里强行解决无缝拼接。
18. 如果本屏 module_type 是 faq_close：优先做简单情感化收口，画面可以是人物享用/放松、家庭生活、产品高级静物；最多保留 1-2 个简短 FAQ 或信任短句；不要重复参数表，不要包装清单，不要复杂规格网格，不要立即购买按钮。
19. typography_lock 是全局字体锁：每屏都必须沿用同一中文无衬线字体风格、标题/正文/标签层级、字重、对齐方式和自然字距；不要每屏换字体气质。
20. 产品身份默认来自最终出图节点的 Image 1。蓝图只描述产品的画面角色和信息任务，不要用长篇文字重新定义产品外观、结构、标签细节或参考图没有的复杂角度。
21. 相邻屏不要复用同一种视觉组织方式；避免连续出现“上方主图 + 下方卡片”“主图 + 两个小卡片”“三张等宽卡片”“底部参数条”。同一套内容可以用全幅场景、局部微距、斜向构图、沉浸式背景、融入式标签、留白标题、竖向动线或轻量浮层来变化表达。
22. 人物/场景屏必须表达明确痛点或使用结果，不要只做“戴着产品”的展示照；优先让人物动作、姿态、环境说明为什么需要它或使用后变轻松。
23. slice_id 为 "02"、"03"、"04" 时必须强制分镜差异：02=首屏卖点延续/生活方式承接，03=机制/能量流/结构证据，04=第二功能结果/模式序列/人物使用结果。禁止 02-04 都出现白色信息卡片、产品居中大图、底部说明卡或相似图标组；第 02 屏禁止黑白痛点对比、旧方案缺陷页和灰暗负面场景。
24. slice_id 为 "02" 时禁止做“产品卡片说明页”；不要把产品图放在白色卡片里讲功能。slice_id 为 "03" 和 "04" 不能使用同角度产品特写、同色光效、同样标签位置。
25. 禁止把内部结构标签写成可见文案：FAQ、FAQ/收口、收口、收口页、总结页、品牌收口、场景页、卖点页、功能页、细节、步骤、参数、参数/信任、参数页、信任页、模块名等不能出现在 text_exact、headline、subheadline、tags、primary_module.message 或 secondary_modules.message。
25A. 不允许用“细节”“功能”“参数/信任”这种单个模块词当标题；必须写成消费者能读懂的利益文案，例如“看得见的用心细节”“买得放心，用得安心”。
26. 如果最后一屏没有明确文案内容，text_exact 可以为空；用产品氛围收尾、生活方式场景、礼物感静物或品牌情绪画面收束，不要强行写 FAQ，也不要写“FAQ/收口”。

请只返回合法 JSON。
